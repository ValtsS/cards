Cards

Git:

`git clone https://github.com/ValtsS/cards.git`
`git checkout module01`


Install:

`npm install`

Test coverage:

`npm run coverage`

Build:

`npm run build`

Run:

`npm run dev`

or

`npm run preview`
